{%- if date -%}
<span class="meta-date">{{ date | date: "%b %d, %Y", "UTC" }}</span>
{%- endif -%}
{%- if extra -%}
<span class="meta-extra">{{ extra }}</span>
{%- endif -%}
<span class="meta-tags">
  {% if tags %}
  {%- for tag in tags -%}
    <a href="/tags/{{ tag }}">#{{ tag }}</a>
    {%- if forloop.last == false -%}&nbsp;{%- endif -%}
  {%- endfor -%}
  {% endif %}
</span>